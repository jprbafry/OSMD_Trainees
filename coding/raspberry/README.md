# Raspberry

Code running on raspberry for a dashboard and other GUI components (with  extra tools)

To run the demos:
```bash
python -m dash_pygame.dashboard -s
python -m dash_pygame.mockup_sensors -s
...
```